nelmio_api_doc:
    documentation:
        info:
            title: MindAudit AI-Compliance API
            description: Interface d'audit intelligente et gestion de conformité.
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas: # default area covers all routes except those starting with /admin/
        path_patterns:
            - ^/api/
            - ^/admin/calendar
